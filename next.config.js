/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.google.co.uk','shorturl.at','www.lh3.google.com'],
  },
}

module.exports = nextConfig
